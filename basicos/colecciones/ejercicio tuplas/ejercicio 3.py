tupla_vacia = ()  # Las tuplas son inmutables, por lo que no se pueden modificar.
# Para "agregar" valores, hay que redefinir la tupla completamente:
tupla_vacia = (1, 2, 3)  # Ejemplo de redefinición
print(tupla_vacia)

